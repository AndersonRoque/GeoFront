import React, { Component } from 'react';
import { Button } from 'antd';

const RendererButton = () => (
    <div>
        <Button type="danger">Run!</Button>
    </div>
);

export default RendererButton;